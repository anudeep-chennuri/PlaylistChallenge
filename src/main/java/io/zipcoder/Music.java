package io.zipcoder;

public class Music {

    private String[] playList;

    public Music(String[] playList){

        int startIndex = Integer.parseInt(playList[startIndex]);
        int endq = Integer.parseInt(playList[endq]);


         public static int shortestDist(startIndex, endq){

            int backwards = ((startIndex - endq) + playList.length) % playList.length;
            int forewards = Math.abs(startIndex - endq);

            if (backwards < forewards) {
                System.out.println(backwards);
            } else System.out.println(forewards);
        }

    }

    public Integer selection(Integer startIndex, String selection){

        startIndex =




        return null;
    }
}
